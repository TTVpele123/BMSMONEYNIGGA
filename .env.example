# Copy to .env.local. Never commit secrets.

OUTBOUND_MODE=dry_run
KILL_SWITCH=false
BMSM_PORT=3222
BMSM_DATA_DIR=

# Identity is locked in lib/email/address.ts — not this env var.
# Authorized mailbox: saevitzonoverstock@gmail.com
# Berkeley and personal mailboxes are denied. saefamoverstock is capped / not the live sender.

# Optional — reuse existing Google OAuth client. Not required for dry-run.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GMAIL_REDIRECT_URI=http://localhost:3222/api/gmail/oauth/callback
BMSM_SECRET=

# Optional xAI API for extraction/copy. Does NOT refill Cursor Grok Bot quota.
XAI_API_KEY=
XAI_MODEL=grok-4

# Legacy Deal OS (read-only import)
LEGACY_DB=/Users/baileysaevitzon/.buzz/var/oliver-deal-os/data/deal-os.db
LEGACY_GOODS=/Users/baileysaevitzon/Desktop/Oliver_Wholesale_Buyer_Research/GOODS
